<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <% genres.map((genre) => { %>
        <a href="/genre/<%= genre.id %>"><%= genre.name %></a>
    <% }) %> <br>
    <a href="/create_genre">Create Genre</a>
</body>
</html>