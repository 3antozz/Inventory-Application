<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <% genres.map((genre) => { %>
        <a href="/genre/<%= genre.id %>"><%= genre.name %></a>
        <a href="/genre/edit/<%= genre.id %>">Edit</a> <br>
    <% }) %> <br>
    <a href="/genre/create">Create Genre</a>
</body>
</html>