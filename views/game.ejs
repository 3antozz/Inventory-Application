<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <% game.map((game) => { %>
        <h1><%= game.name %> <br> <%= game.description %> <br> <%= game.release_date %> <br> <%= game.quantity %> <br> <%= game.developers.join(', ') %> <br> <%= game.genres.join(', ') %></h1>
    <% }) %>
</body>
</html>