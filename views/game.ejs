<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/public/styles/choices.min.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Farro:wght@300;400;500;700&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles/reset.css">
    <link rel="stylesheet" href="/styles/navbar.css">
</head>
<body>
    <%- include('./partials/Navbar.ejs') %>
    <br>
    <% game.map((game) => { %>
        <img src="<%= game.cover_url %>" alt="Cover image of <%= game.name %>"> <h1><%= game.name %> <br> <%= game.description %> <br> <%= game.release_date %> <br> <%= game.quantity %> <br> <%= game.price %> <br> <%= game.developers.join(', ') %> <br> <%= game.genres.join(', ') %></h1>
    <% }) %>
</body>
</html>